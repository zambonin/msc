\documentclass{standalone}

\usepackage{pgfplots}

\pgfplotsset{compat=newest}

\begin{document}

\begin{tikzpicture}
  \begin{axis}[xlabel = $q$, yticklabel = {
      \pgfmathparse{\tick*100}\pgfmathprintnumber{\pgfmathresult}\%
    }, legend pos = south east, legend style = {
      draw = none, font = \footnotesize
    },
  ]
    \addplot[densely dotted, thick] gnuplot [raw gnuplot] {
      plot 'prob.txt' using 1:3 every 36::360
    };
    \addlegendentry{$\Pi(q, n)$}
    \addplot[densely dashed, thick] gnuplot [raw gnuplot] {
      plot 'prob.txt' using 1:6 every 36::360
    };
    \addlegendentry{$\widetilde{\Pi}(q, n)$}
  \end{axis}
\end{tikzpicture}

\end{document}
