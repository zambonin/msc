\documentclass{standalone}

\usepackage{pgfplots}

\pgfplotsset{compat=newest}

\begin{document}

\begin{tikzpicture}
  \begin{axis}[
    view = {135}{30}, grid = both, grid style = {dotted}, zmax = 10,
    ticklabel style = {font = \tiny}, minor x tick num = 2,
    minor y tick num = 2, colormap = {bw}{gray(0cm) = (1); gray(1cm) = (0)},
    label style = {sloped}, zlabel style = {rotate = 270}, xlabel = $q$,
    ylabel = $t$, zlabel = $d_{\sigma}^{t}$, xtick = {2, 32, 64, 128, 256},
  ]
    \addplot3 [surf] gnuplot [raw gnuplot] {
      set dgrid3d 48, 48, 48;
      splot 'q256-t512-n42.txt' using 1:2:8 with lines
    };
  \end{axis}
\end{tikzpicture}

\end{document}
